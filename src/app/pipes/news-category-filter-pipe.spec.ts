import { NewsCategoryFilterPipe } from './news-category-filter-pipe';

describe('NewsCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
