import { NewsTextSearchPipe } from './news-text-search-pipe';

describe('NewsTextSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsTextSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
